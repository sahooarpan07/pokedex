import {StyleSheet} from 'react-native';
export const styles = StyleSheet.create({
  container: {
    borderRadius: 6,
    padding: 4,
    borderColor: '#180345',
    borderWidth: 2,
  },
  value: {color: '#180345', fontSize: 12, fontWeight: '500'},
});
